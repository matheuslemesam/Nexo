# Nexo Server - FastAPI

Backend API for Nexo application.

## Setup

```bash
cd server
python -m venv venv
source venv/bin/activate  # Linux/macOS
pip install -r requirements.txt
```

## Run

```bash
uvicorn main:app --reload
```

API docs: http://localhost:8000/docs
